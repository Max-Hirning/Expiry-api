/**
 * Fastify plugin names
 */
export enum FastifyPlugin {
    Prisma = "prisma",
    Env = "env",
    Jwt = "jwt",
    Awilix = "awilix",
    TokenCookies = "token-cookies",
    SocketIO = "socket-io",
}
